stages:
  - production

image: tetraweb/php:7.1

production-env:
  stage: production
  artifacts:
    paths:
    - ./build_woodoo-buildtools.tar.gz
    expire_in: 3 month
  script:
    - node -v
    - composer.phar validate
    - npm -v
    - npm outdated
    - npm install
  only:
    - master
